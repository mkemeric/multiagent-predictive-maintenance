# MongoDB Multi-Agent Predictive Maintenance
# HPE Private Cloud AI MLIS Configuration

# ============================================
# MongoDB Atlas Configuration
# ============================================
MONGODB_URI="<your-mongodb-uri>"
DATABASE_NAME="agentic_predictive_maintenance"

# ============================================
# HPE PC AI MLIS Configuration
# ============================================
# Base URL for MLIS OpenAI-compatible API endpoint
# Example: https://your-mlis-endpoint.example.com/v1
MLIS_BASE_URL="<your-mlis-endpoint>/v1"

# API Key for MLIS authentication (if required by your deployment)
MLIS_API_KEY=""

# ============================================
# Model Configuration
# ============================================
# Foundational model for chat/completion (NVIDIA NIM)
# Recommended: meta/llama-3.1-70b-instruct (supports tool calling)
# Alternative: meta/llama-3.1-8b-instruct (smaller, still supports tools)
COMPLETION_MODEL="meta/llama-3.1-70b-instruct"

# Embedding model for vector search (NVIDIA NeMo Retriever)
# Recommended: nvidia/nv-embedqa-e5-v5 (1024 dimensions, matches existing config)
# Alternative: nvidia/llama-3.2-nv-embedqa-1b-v2 (configurable dimensions)
EMBEDDING_MODEL="nvidia/nv-embedqa-e5-v5"

# ============================================
# DEPRECATED - AWS Bedrock Configuration (removed)
# ============================================
# The following variables are no longer used:
# AWS_REGION="us-east-1"
# AWS_PROFILE="default"
# COMPLETION_MODEL="us.anthropic.claude-3-5-haiku-20241022-v1:0"
# EMBEDDING_MODEL="cohere.embed-english-v3"
